import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flexGrow: 1,
      padding: 20,
      backgroundColor: '#fff',
    },
    textAreaPortrait: {
      height: 200,
      borderColor: '#ccc',
      borderWidth: 1,
      padding: 10,
      margin:10,
      borderRadius: 5,
      fontSize: 16,
    },
    textAreaLandscape: {
      flex: 1,
      borderColor: '#ccc',
      borderWidth: 1,
      padding: 10,
      margin:10,
      borderRadius: 5,
      fontSize: 16,
    },
  });

  